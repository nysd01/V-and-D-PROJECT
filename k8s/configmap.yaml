apiVersion: v1
kind: ConfigMap
metadata:
  name: internconnect-config
  namespace: internconnect
data:
  PORT: "3000"
  NODE_ENV: "production"
  # Frontend API URL — uses K8s service discovery (backend-service)
  EXPO_PUBLIC_API_URL: "http://backend-service:3000/api"
